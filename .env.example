DISCORD_TOKEN=
SONGPORT_API_KEY=

# Optional. Set it to one server id while you test.
# The /sl command then appears in that server in seconds instead of up to one hour.
# Leave it empty in production to register the command in every server.
DISCORD_GUILD_ID=
